# Core RL and Learning Libraries
torch>=2.0.0; sys_platform == 'darwin'  # For MPS support on Mac
torch>=2.0.0; sys_platform != 'darwin'  # General torch for Linux/Cluster
stable-baselines3[extra]>=2.0.0
gymnasium[atary,accept-rom-license]>=0.28.1
shimmy>=1.3.0 # For compatibility if needed

# Game Environment
pygame>=2.5.0
opencv-python>=4.8.0 # For image preprocessing

# Utilities
numpy>=1.24.0
matplotlib # For visualization if needed
